import { NodeViewProps } from "@tiptap/react";

/**
 * Placeholder component for CodeBlockPreview - not used in config pages
 */
export const CodeBlockPreview = (_props: NodeViewProps) => {
  return null;
};
